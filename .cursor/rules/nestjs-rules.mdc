# 🏗️ NestJS Правила разработки

## 📚 Документация

### **Загруженные документации (приоритет):**
- **@NestJS** - используй для всех вопросов по NestJS
- **@TypeScript** - для вопросов по типизации
- **@gRPC** - для вопросов по микросервисам и gRPC

### **Статические ссылки (резерв):**
- **[NestJS Documentation](https://docs.nestjs.com/)** - Основная документация
- **[NestJS Microservices](https://docs.nestjs.com/microservices/basics)** - Микросервисы
- **[NestJS gRPC](https://docs.nestjs.com/microservices/grpc)** - gRPC интеграция
- **[NestJS Testing](https://docs.nestjs.com/fundamentals/testing)** - Тестирование
- **[NestJS Validation](https://docs.nestjs.com/techniques/validation)** - Валидация
- **[NestJS Caching](https://docs.nestjs.com/techniques/caching)** - Кэширование
- **[NestJS Security](https://docs.nestjs.com/security/authentication)** - Безопасность
- **[NestJS Configuration](https://docs.nestjs.com/techniques/configuration)** - Конфигурация
- **[NestJS Database](https://docs.nestjs.com/techniques/database)** - Работа с БД
- **[NestJS Exception Filters](https://docs.nestjs.com/exception-filters)** - Фильтры исключений
- **[NestJS Interceptors](https://docs.nestjs.com/interceptors)** - Перехватчики
- **[NestJS Pipes](https://docs.nestjs.com/pipes)** - Трубопроводы
- **[NestJS Guards](https://docs.nestjs.com/guards)** - Защитники

## 🎯 Основные принципы

### Архитектура модулей
- Используй модульную архитектуру
- Инкапсулируй API в модули
- Один модуль на основной домен/маршрут
- Один контроллер для основного маршрута
- Папка models с типами данных
- Сервисы с бизнес-логикой и персистентностью

### Структура проекта
```
src/
├── core/                    # NestJS артефакты
│   ├── filters/            # Глобальные фильтры исключений
│   ├── guards/             # Guards для управления правами
│   ├── interceptors/       # Interceptors для управления запросами
│   └── middlewares/        # Глобальные middleware
├── shared/                 # Общие сервисы между модулями
│   ├── utils/             # Утилиты
│   └── services/          # Общая бизнес-логика
├── modules/               # Основные модули
│   ├── products/          # Модуль продуктов
│   │   ├── controllers/   # Контроллеры
│   │   ├── services/      # Сервисы
│   │   ├── models/        # DTOs и типы
│   │   └── entities/      # Сущности MikroORM
│   └── auth/              # Модуль аутентификации
└── main.ts                # Точка входа
```

## 📝 TypeScript Guidelines

> **Примечание**: Детальные правила TypeScript смотри в `typescript-rules.mdc`

### Базовые принципы для NestJS
- Используй английский для кода и документации
- Всегда объявляй типы для переменных и функций
- Избегай `any`, создавай необходимые типы
- Используй JSDoc для публичных классов и методов
- Не оставляй пустых строк внутри функций
- Один экспорт на файл

## 🏗️ NestJS Специфика

### Контроллеры
```typescript
// ✅ Один контроллер на маршрут
@Controller('products')
export class ProductsController {
  constructor(private readonly productsService: ProductsService) {}

  @Get()
  async findAll(): Promise<Product[]> {
    return this.productsService.findAll()
  }

  @Post()
  async create(@Body() createProductDto: CreateProductDto): Promise<Product> {
    return this.productsService.create(createProductDto)
  }

  @Get('admin/test')
  async test(): Promise<{ status: string }> {
    return { status: 'OK' }
  }
}
```

### Сервисы
```typescript
// ✅ Один сервис на сущность
@Injectable()
export class ProductsService {
  constructor(
    private readonly productsRepository: ProductsRepository,
    private readonly logger: Logger
  ) {}

  async findAll(): Promise<Product[]> {
    try {
      return await this.productsRepository.findAll()
    } catch (error) {
      this.logger.error('Failed to fetch products', error)
      throw new InternalServerErrorException('Failed to fetch products')
    }
  }

  async create(createProductDto: CreateProductDto): Promise<Product> {
    const product = this.productsRepository.create(createProductDto)
    return await this.productsRepository.save(product)
  }
}
```

### DTOs и валидация
```typescript
// ✅ DTOs с валидацией для входных данных
export class CreateProductDto {
  @IsString()
  @IsNotEmpty()
  @Length(1, 255)
  name: string

  @IsNumber()
  @IsPositive()
  price: number

  @IsString()
  @IsIn(['videocards', 'processors', 'motherboards'])
  category: string
}

// ✅ Простые типы для выходных данных
export interface ProductResponse {
  id: string
  name: string
  price: number
  category: string
  createdAt: Date
}
```

### Модули
```typescript
// ✅ Модульная архитектура
@Module({
  imports: [
    MikroOrmModule.forFeature([Product]),
    SharedModule
  ],
  controllers: [ProductsController],
  providers: [ProductsService, ProductsRepository],
  exports: [ProductsService]
})
export class ProductsModule {}
```

## 🗄️ Работа с данными

### Сущности MikroORM
```typescript
// ✅ Сущности для персистентности данных
@Entity()
export class Product {
  @PrimaryKey()
  id: string = crypto.randomUUID()

  @Property()
  name: string

  @Property()
  price: number

  @Property()
  category: string

  @Property()
  createdAt: Date = new Date()

  @Property({ nullable: true })
  updatedAt?: Date
}
```

### Repository Pattern
```typescript
// ✅ Repository для работы с данными
@Injectable()
export class ProductsRepository {
  constructor(
    @InjectRepository(Product)
    private readonly productRepository: EntityRepository<Product>
  ) {}

  async findAll(): Promise<Product[]> {
    return this.productRepository.findAll()
  }

  async findById(id: string): Promise<Product | null> {
    return this.productRepository.findOne({ id })
  }

  async create(data: CreateProductDto): Promise<Product> {
    const product = this.productRepository.create(data)
    return this.productRepository.persistAndFlush(product)
  }
}
```

## 🔒 Безопасность и обработка ошибок

### Guards
```typescript
// ✅ Guards для управления правами
@Injectable()
export class AuthGuard implements CanActivate {
  canActivate(context: ExecutionContext): boolean {
    const request = context.switchToHttp().getRequest()
    return this.validateRequest(request)
  }

  private validateRequest(request: any): boolean {
    // Логика валидации
    return true
  }
}
```

### Filters
```typescript
// ✅ Глобальные фильтры исключений
@Catch()
export class GlobalExceptionFilter implements ExceptionFilter {
  catch(exception: unknown, host: ArgumentsHost) {
    const ctx = host.switchToHttp()
    const response = ctx.getResponse()
    const request = ctx.getRequest()

    const status = exception instanceof HttpException
      ? exception.getStatus()
      : HttpStatus.INTERNAL_SERVER_ERROR

    response.status(status).json({
      statusCode: status,
      timestamp: new Date().toISOString(),
      path: request.url,
      message: exception instanceof HttpException
        ? exception.message
        : 'Internal server error'
    })
  }
}
```

## 🧪 Тестирование

### Unit тесты
```typescript
// ✅ Arrange-Act-Assert
describe('ProductsService', () => {
  let service: ProductsService
  let mockRepository: jest.Mocked<ProductsRepository>

  beforeEach(() => {
    mockRepository = {
      findAll: jest.fn(),
      create: jest.fn()
    } as any

    service = new ProductsService(mockRepository)
  })

  describe('findAll', () => {
    it('should return all products', async () => {
      // Arrange
      const inputProducts = [
        { id: '1', name: 'RTX 5070', price: 50000 }
      ]
      const expectedProducts = inputProducts
      mockRepository.findAll.mockResolvedValue(inputProducts)

      // Act
      const actualProducts = await service.findAll()

      // Assert
      expect(actualProducts).toEqual(expectedProducts)
      expect(mockRepository.findAll).toHaveBeenCalledTimes(1)
    })
  })
})
```

### E2E тесты
```typescript
// ✅ Given-When-Then
describe('Products API (e2e)', () => {
  let app: INestApplication

  beforeEach(async () => {
    const moduleFixture = await Test.createTestingModule({
      imports: [AppModule]
    }).compile()

    app = moduleFixture.createNestApplication()
    await app.init()
  })

  it('/products (GET) should return products', async () => {
    // Given
    const expectedStatus = 200

    // When
    const response = await request(app.getHttpServer())
      .get('/products')
      .expect(expectedStatus)

    // Then
    expect(response.body).toBeInstanceOf(Array)
  })
})
```

## 📊 Логирование и мониторинг

### Interceptors
```typescript
// ✅ Interceptors для логирования
@Injectable()
export class LoggingInterceptor implements NestInterceptor {
  constructor(private readonly logger: Logger) {}

  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {
    const request = context.switchToHttp().getRequest()
    const method = request.method
    const url = request.url

    this.logger.log(`${method} ${url}`)

    const now = Date.now()
    return next.handle().pipe(
      tap(() => {
        this.logger.log(`${method} ${url} - ${Date.now() - now}ms`)
      })
    )
  }
}
```

## 🚨 Лучшие практики

### Производительность
- Используй кэширование для часто запрашиваемых данных
- Применяй пагинацию для больших списков
- Оптимизируй запросы к базе данных
- Используй connection pooling

### Безопасность
- Валидируй все входные данные
- Используй prepared statements
- Не храни секреты в коде
- Применяй rate limiting
- Логируй подозрительную активность

### Код
- Следуй SOLID принципам
- Используй dependency injection
- Пиши чистый, читаемый код
- Добавляй комментарии к сложной логике
- Регулярно рефактори код

## 🚨 Чего избегать

- ❌ Не используй `any` тип
- ❌ Не делай функции длиннее 20 инструкций
- ❌ Не создавай классы больше 200 строк
- ❌ Не игнорируй обработку ошибок
- ❌ Не забывай про валидацию входных данных
- ❌ Не используй глобальные переменные
- ❌ Не делай все в одном модуле

## 🔗 Полезные ссылки

### **Официальная документация:**
- **[NestJS Docs](https://docs.nestjs.com/)** - Полная документация
- **[NestJS GitHub](https://github.com/nestjs/nest)** - Исходный код
- **[NestJS Examples](https://github.com/nestjs/nest/tree/master/sample)** - Примеры кода

### **Сообщество:**
- **[NestJS Discord](https://discord.gg/nestjs)** - Discord сервер
- **[NestJS Stack Overflow](https://stackoverflow.com/questions/tagged/nestjs)** - Вопросы и ответы
- **[NestJS Blog](https://blog.nestjs.com/)** - Блог команды

### **Инструменты:**
- **[NestJS CLI](https://docs.nestjs.com/cli/overview)** - Командная строка
- **[NestJS Schematics](https://docs.nestjs.com/cli/usages#nest-generate)** - Генераторы кода
- **[NestJS Testing](https://docs.nestjs.com/fundamentals/testing)** - Тестирование

### **Для вашего проекта MarketVision:**
- **[NestJS gRPC](https://docs.nestjs.com/microservices/grpc)** - Для WB API и Ozon API
- **[NestJS Caching](https://docs.nestjs.com/techniques/caching)** - Для Redis кэширования
- **[NestJS Validation](https://docs.nestjs.com/techniques/validation)** - Для DTO валидации
description:
globs:
alwaysApply: false
---
