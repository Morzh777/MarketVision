# 🛠️ Правила разработки MarketVision

## 🤖 Инструкции для ИИ

**Контекст проекта:**
- Микросервисная архитектура для мониторинга цен
- gRPC коммуникация между сервисами
- Парсинг данных с маркетплейсов (Wildberries, Ozon)
- Агрегация и фильтрация продуктов
- **Фронтенд**: Next.js приложение для визуализации аналитики
- **Аналитика**: графики цен, сравнения, тренды, уведомления

**Приоритеты:**
1. Надежность и стабильность
2. Производительность парсинга
3. Масштабируемость
4. Легкость поддержки
5. **UX аналитики** - интуитивные графики и дашборды

**При работе с кодом:**
1. Всегда проверяй соответствие SOLID принципам
2. Сначала проектируй интерфейсы, потом реализацию
3. Используй TypeScript строгую типизацию везде (см. `typescript-rules.mdc`)
4. Применяй паттерны из раздела "Проектирование"
5. Проверяй безопасность входных данных
6. Оптимизируй производительность критических участков
7. Пиши тесты для новой функциональности

**При работе с документацией:**
1. **Всегда используй загруженные документации** через `@` ссылки
2. **@NestJS** - для вопросов по NestJS, микросервисам, gRPC
3. **@TypeScript** - для вопросов по TypeScript, типизации
4. **@Python** - для вопросов по Python, асинхронности
5. **@Docker** - для вопросов по контейнеризации
6. **@gRPC** - для вопросов по Protocol Buffers, микросервисам
7. **@SCSS** - для вопросов по SCSS, стилизации, CSS переменным
8. **Используй актуальную документацию** вместо статических ссылок
9. **Приводи примеры из официальной документации**

**При рефакторинге:**
- Выявляй нарушения SOLID
- Разбивай большие классы на маленькие
- Выноси общую логику в абстракции
- Используй dependency injection

**При создании новых сервисов:**
- Следуй архитектуре проекта
- Используй соответствующие технологии
- Применяй правильные паттерны
- Добавляй валидацию и обработку ошибок
- Следуй детальным правилам NestJS в `nestjs-rules.mdc`

**При работе с фронтендом:**
- Фокус на UX аналитики и визуализации
- Оптимизируй рендеринг графиков
- Используй кэширование для данных
- Адаптивный дизайн для дашбордов
- Следуй детальным правилам Next.js в `nextjs-rules.mdc`
- Следуй детальным правилам SCSS в `scss-rules.mdc`

**При работе с gRPC:**
- Следуй детальным правилам gRPC в `grpc-rules.mdc`
- Используй Protocol Buffers для типизации
- Правильно обрабатывай ошибки
- Мониторь производительность

**При работе с REST API:**
- Следуй детальным правилам REST API в `rest-api-rules.mdc`
- Используй правильные HTTP методы и статус коды
- Валидируй входные данные через DTO
- Добавляй документацию и тесты

**При работе с документацией:**
- **Всегда используй загруженные документации** через `@` ссылки
- **@NestJS** - для вопросов по NestJS, микросервисам, gRPC
- **@TypeScript** - для вопросов по TypeScript, типизации
- **@Python** - для вопросов по Python, асинхронности
- **@Docker** - для вопросов по контейнеризации
- **@gRPC** - для вопросов по Protocol Buffers, микросервисам
- **Используй актуальную документацию** вместо статических ссылок
- **Приводи примеры из официальной документации**

**При работе с Docker:**
- Следуй детальным правилам Docker в `docker-rules.mdc`
- Используй многоэтапную сборку
- Настраивай health checks
- Оптимизируй размер образов

**При работе с Python:**
- Следуй детальным правилам Python в `python-rules.mdc`
- Используй строгую типизацию с mypy
- Применяй async/await для I/O операций
- Следуй Clean Architecture

## 🚀 Технологии
- **Backend**: NestJS, Node.js, TypeScript, gRPC, PostgreSQL
- **Frontend**: ReactJS, NextJS, TypeScript, TailwindCSS  
- **Python**: FastAPI, gRPC, Selenium
- **DevOps**: Docker, Docker Compose

## 🏗️ Архитектура

### SOLID принципы
- **S**ingle Responsibility: один класс = одна ответственность
- **O**pen/Closed: открыт для расширения, закрыт для изменения
- **L**iskov Substitution: наследники заменяют базовые классы
- **I**nterface Segregation: много маленьких интерфейсов
- **D**ependency Inversion: зависимости от абстракций

### Проектирование
- Сначала интерфейсы и абстракции
- Композиция вместо наследования
- Разделяй бизнес-логику и инфраструктуру
- Паттерны: Repository, Factory, Strategy, Observer
- Избегай god objects

## 🔒 Безопасность
- Валидируй входные данные (см. `typescript-rules.mdc` для деталей)
- Prepared statements для SQL
- Не храни секреты в коде
- HTTPS для API
- Sanitize данные

## ⚡ Производительность
- Оптимизируй запросы к БД
- Кэширование
- Пагинация
- Lazy loading
- Минимизируй bundle size

## 🗄️ База данных
- Миграции для изменений схемы
- Правильные индексы
- Избегай N+1 запросов
- Транзакции для критических операций
- Мониторинг запросов

## 🧪 Тестирование
- Unit тесты для бизнес-логики
- Integration тесты для API
- E2E для критических сценариев
- Mock внешние зависимости
- Покрытие > 80%

## 📝 Общие принципы
- DRY принцип
- Читаемость > производительность
- Без TODO и заглушек
- Early returns
- Описательные имена
- Proper error handling
- Dependency injection
- **TypeScript**: см. детальные правила в `typescript-rules.mdc`

## 🎯 Специфика технологий

### NestJS
- Декораторы, модули, DTOs, DI
- См. детальные правила в `nestjs-rules.mdc`

### React
- Const компоненты, handle префикс
- См. детальные правила в `nextjs-rules.mdc`

### SCSS
- BEM методология, CSS переменные, миксины
- См. детальные правила в `scss-rules.mdc`

### TypeScript
- Интерфейсы, abstract classes
- См. детальные правила в `typescript-rules.mdc`

### gRPC
- Правильная структура proto
- См. детальные правила в `grpc-rules.mdc`

### Docker
- Оптимизированные образы
- См. детальные правила в `docker-rules.mdc`

### Python
- Async/await, mypy, Clean Architecture
- См. детальные правила в `python-rules.mdc`

### REST API
- Стандарты эндпоинтов, DTO, валидация
- См. детальные правила в `rest-api-rules.mdc`

