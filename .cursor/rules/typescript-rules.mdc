# 📘 TypeScript Правила разработки

## 📚 Документация

### **Загруженные документации (приоритет):**
- **@TypeScript** - используй для всех вопросов по TypeScript
- **@NestJS** - для вопросов по NestJS и типизации

### **Статические ссылки (резерв):**
- **[TypeScript Handbook](https://www.typescriptlang.org/docs/)** - Полная документация
- **[TypeScript Playground](https://www.typescriptlang.org/play)** - Онлайн редактор для экспериментов
- **[TypeScript GitHub](https://github.com/microsoft/TypeScript)** - Исходный код
- **[TypeScript Config](https://www.typescriptlang.org/tsconfig)** - Конфигурация tsconfig.json
- **[TypeScript Compiler API](https://github.com/microsoft/TypeScript/wiki/Using-the-Compiler-API)** - API компилятора
- **[TypeScript ESLint](https://typescript-eslint.io/)** - ESLint для TypeScript
- **[TypeScript Performance](https://github.com/microsoft/TypeScript/wiki/Performance)** - Оптимизация производительности

## 🎯 Основные принципы

### Строгая типизация
- Всегда объявляй типы для переменных и функций
- Избегай `any` - создавай необходимые типы
- Используй `unknown` вместо `any` для неизвестных типов
- Применяй `strict` режим в `tsconfig.json`

### Номенклатура
```typescript
// ✅ Правильно
interface ProductData {}
type ProductStatus = 'active' | 'inactive'
class ProductService {}
const productName: string = 'RTX 5070'
const isProductAvailable: boolean = true
const handleProductCreate = (): void => {}

// ❌ Неправильно
interface productData {}
type product_status = string
class productService {}
const ProductName = 'RTX 5070'
const product_available = true
const productCreate = () => {}
```

## 🏗️ Типы и интерфейсы

### Интерфейсы
```typescript
// ✅ Описывай контракты через интерфейсы
interface Product {
  id: string
  name: string
  price: number
  category: ProductCategory
  createdAt: Date
  updatedAt?: Date
}

// ✅ Разделяй интерфейсы по ответственности
interface ProductCreateRequest {
  name: string
  price: number
  category: ProductCategory
}

interface ProductResponse {
  id: string
  name: string
  price: number
  category: ProductCategory
  createdAt: Date
}
```

### Типы
```typescript
// ✅ Используй типы для union и intersection
type ProductCategory = 'videocards' | 'processors' | 'motherboards'
type ProductStatus = 'active' | 'inactive' | 'deleted'

// ✅ Типы для функций
type ProductValidator = (product: Product) => boolean
type ProductMapper = (product: Product) => ProductResponse

// ✅ Utility типы
type ProductUpdateRequest = Partial<ProductCreateRequest>
type ProductId = Pick<Product, 'id'>
type ProductWithoutId = Omit<Product, 'id'>
```

### Enum vs Union Types
```typescript
// ✅ Предпочитай union types
type ProductCategory = 'videocards' | 'processors' | 'motherboards'

// ❌ Избегай enum для простых значений
enum ProductCategory {
  Videocards = 'videocards',
  Processors = 'processors',
  Motherboards = 'motherboards'
}

// ✅ Используй enum только для сложных случаев
enum HttpStatus {
  OK = 200,
  NotFound = 404,
  InternalError = 500
}
```

## 🔧 Функции

### Сигнатуры функций
```typescript
// ✅ Явные типы параметров и возвращаемого значения
const validateProduct = (product: Product): boolean => {
  return product.name.length > 0 && product.price > 0
}

// ✅ Функции с несколькими параметрами используй RO-RO
interface CreateProductParams {
  name: string
  price: number
  category: ProductCategory
}

interface CreateProductResult {
  success: boolean
  product?: Product
  error?: string
}

const createProduct = (params: CreateProductParams): CreateProductResult => {
  // implementation
}
```

### Arrow функции
```typescript
// ✅ Arrow функции для простых операций (< 3 инструкций)
const getProductName = (product: Product): string => product.name
const isExpensive = (product: Product): boolean => product.price > 100000

// ✅ Named функции для сложной логики
function processProductData(products: Product[]): ProcessedData {
  // Сложная логика обработки
  return processedData
}
```

### Overloads
```typescript
// ✅ Function overloads для разных сигнатур
function getProduct(id: string): Product | null
function getProduct(category: ProductCategory): Product[]
function getProduct(param: string | ProductCategory): Product | Product[] | null {
  if (typeof param === 'string') {
    return findProductById(param)
  }
  return findProductsByCategory(param)
}
```

## 🏛️ Классы

### SOLID принципы
```typescript
// ✅ Single Responsibility
class ProductValidator {
  validate(product: Product): ValidationResult {
    // Только валидация
  }
}

class ProductRepository {
  save(product: Product): Promise<Product> {
    // Только сохранение
  }
}

// ✅ Dependency Inversion
interface ProductRepository {
  save(product: Product): Promise<Product>
  findById(id: string): Promise<Product | null>
}

class ProductService {
  constructor(private repository: ProductRepository) {}
  
  async createProduct(product: Product): Promise<Product> {
    return this.repository.save(product)
  }
}
```

### Абстрактные классы
```typescript
// ✅ Абстрактные классы для общей логики
abstract class BaseValidator<T> {
  abstract validate(data: T): ValidationResult
  
  protected logValidationError(error: string): void {
    console.error(`Validation error: ${error}`)
  }
}

class ProductValidator extends BaseValidator<Product> {
  validate(product: Product): ValidationResult {
    if (!product.name) {
      this.logValidationError('Product name is required')
      return { isValid: false, errors: ['Product name is required'] }
    }
    return { isValid: true, errors: [] }
  }
}
```

## 📊 Generics

### Generic типы
```typescript
// ✅ Generic интерфейсы
interface Repository<T> {
  findById(id: string): Promise<T | null>
  save(entity: T): Promise<T>
  delete(id: string): Promise<void>
}

// ✅ Generic классы
class Cache<T> {
  private data: Map<string, T> = new Map()
  
  set(key: string, value: T): void {
    this.data.set(key, value)
  }
  
  get(key: string): T | undefined {
    return this.data.get(key)
  }
}

// ✅ Generic функции
const createArray = <T>(length: number, value: T): T[] => {
  return Array(length).fill(value)
}
```

### Constraints
```typescript
// ✅ Generic constraints
interface Identifiable {
  id: string
}

class EntityRepository<T extends Identifiable> {
  findById(id: string): Promise<T | null> {
    // implementation
  }
}

// ✅ Multiple constraints
interface Product extends Identifiable, Timestamped {
  name: string
  price: number
}

interface Timestamped {
  createdAt: Date
  updatedAt?: Date
}
```

## 🔒 Безопасность типов

### Type Guards
```typescript
// ✅ Type guards для runtime проверок
const isProduct = (obj: unknown): obj is Product => {
  return typeof obj === 'object' && 
         obj !== null && 
         'id' in obj && 
         'name' in obj && 
         'price' in obj
}

// ✅ Discriminated unions
type ProductEvent = 
  | { type: 'created'; product: Product }
  | { type: 'updated'; product: Product; changes: Partial<Product> }
  | { type: 'deleted'; productId: string }

const handleProductEvent = (event: ProductEvent): void => {
  switch (event.type) {
    case 'created':
      console.log('Product created:', event.product.name)
      break
    case 'updated':
      console.log('Product updated:', event.changes)
      break
    case 'deleted':
      console.log('Product deleted:', event.productId)
      break
  }
}
```

### Assertion Functions
```typescript
// ✅ Assertion functions
function assertIsProduct(obj: unknown): asserts obj is Product {
  if (!isProduct(obj)) {
    throw new Error('Object is not a Product')
  }
}

const processData = (data: unknown): void => {
  assertIsProduct(data)
  // TypeScript знает что data это Product
  console.log(data.name)
}
```

## 🧪 Тестирование типов

### Type Testing
```typescript
// ✅ Type assertions в тестах
type TestProduct = {
  id: string
  name: string
  price: number
}

// Проверяем что Product соответствует TestProduct
type ProductTest = Product extends TestProduct ? true : false
const _test: ProductTest = true

// ✅ Utility типы для тестирования
type RequiredFields = Required<Pick<Product, 'id' | 'name'>>
type OptionalFields = Partial<Pick<Product, 'updatedAt'>>
```

## 📝 JSDoc

### Документирование
```typescript
/**
 * Валидирует продукт по бизнес-правилам
 * @param product - Продукт для валидации
 * @returns Результат валидации с ошибками
 * @throws {ValidationError} Если продукт не прошел валидацию
 */
const validateProduct = (product: Product): ValidationResult => {
  // implementation
}

/**
 * Сервис для работы с продуктами
 */
class ProductService {
  /**
   * Создает новый продукт
   * @param createRequest - Данные для создания продукта
   * @returns Созданный продукт
   */
  async createProduct(createRequest: ProductCreateRequest): Promise<Product> {
    // implementation
  }
}
```

## 🚨 Лучшие практики

### Производительность
- Используй `readonly` для неизменяемых данных
- Применяй `const assertions` для литералов
- Избегай избыточных типов

```typescript
// ✅ Readonly для неизменяемых данных
interface ProductConfig {
  readonly maxPrice: number
  readonly categories: readonly ProductCategory[]
}

// ✅ Const assertions
const PRODUCT_CATEGORIES = ['videocards', 'processors'] as const
type ProductCategory = typeof PRODUCT_CATEGORIES[number]
```

### Читаемость
- Используй описательные имена типов
- Группируй связанные типы
- Добавляй комментарии к сложным типам

```typescript
// ✅ Описательные имена
type ProductPrice = number
type ProductName = string
type ProductId = string

// ✅ Группировка связанных типов
interface Product {
  // Идентификация
  id: ProductId
  name: ProductName
  
  // Ценовая информация
  price: ProductPrice
  currency: 'RUB' | 'USD'
  
  // Метаданные
  createdAt: Date
  updatedAt?: Date
}
```

## 🚨 Чего избегать

- ❌ Не используй `any` тип
- ❌ Не игнорируй ошибки TypeScript
- ❌ Не делай типы слишком сложными
- ❌ Не забывай про generic constraints
- ❌ Не используй `Object` и `Function` типы
- ❌ Не игнорируй strict режим
- ❌ Не создавай типы без необходимости

description:
globs:
alwaysApply: false
---
