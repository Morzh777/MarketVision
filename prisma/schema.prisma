model Product {
  id        String   @id @default(uuid())
  name      String
  price     Float
  category  String
  query     String
  source    String?
  discount  Float?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
} 